import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Mask = () => {
  return (
    <View>
      <Text>Mask</Text>
    </View>
  );
};

export default Mask;

const styles = StyleSheet.create({});
